interface Props {
    handle: string;
    displayName?: string;
    size?: number;
    ring?: boolean;
}
/** Deterministic, generated from the handle — no uploads, no image hosting. */
export declare function Avatar({ handle, displayName, size, ring }: Props): import("react").JSX.Element;
export {};
//# sourceMappingURL=Avatar.d.ts.map